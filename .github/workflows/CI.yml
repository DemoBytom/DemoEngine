# ------------------------------------------------------------------------------
# <auto-generated>
#
#     This code was generated.
#
#     - To turn off auto-generation set:
#
#         [GitHubActionsV2 (AutoGenerate = false)]
#
#     - To trigger manual generation invoke:
#
#         nuke --configure-build-server --host GitHubActions
#
# </auto-generated>
# ------------------------------------------------------------------------------

name: CI

on: [push]

jobs:
  windows-latest:
    name: windows-latest
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - name: Run './build.cmd Clean Compile Test Publish'
        run: ./build.cmd Clean Compile Test Publish
        env:
            GitHubToken: ${{ secrets.GITHUB_TOKEN }}
            CoverallsToken: ${{ secrets.CoverallsToken }}
      - uses: actions/upload-artifact@v1
        with:
          name: coverage.zip
          path: artifacts/coverage.zip
      - uses: actions/upload-artifact@v1
        with:
          name: Demo.Engine.zip
          path: artifacts/Demo.Engine.zip
      - uses: actions/upload-artifact@v1
        with:
          name: Demo.Engine.win10-x64.zip
          path: artifacts/Demo.Engine.win10-x64.zip
